# 开发日志

## 2026-04-28 P0.1 建立最小可运行工程

### 本阶段目标

- 建立 .NET 8 最小可运行解决方案。
- 按规范创建 WebApi、Services、Repository、Models、Common 分层项目。
- 前端作为独立 Vue3 项目创建，可本地单独启动。
- WebAPI 可本地启动，并提供最小健康检查接口。

### 完成内容

- 在 `/src` 下创建 `NetMind.sln`。
- 创建并加入解决方案的 .NET 项目：
  - `NetMind.WebApi`
  - `NetMind.Services`
  - `NetMind.Repository`
  - `NetMind.Models`
  - `NetMind.Common`
- 建立单向项目引用：
  - `WebApi -> Services -> Repository -> Models`
  - `Common` 作为公共层被业务层引用。
- 新增最小健康检查接口：
  - `GET /api/system/health`
- 创建独立前端项目 `/src/NetMind.Frontend`：
  - 使用 `Vue 3.5.22`
  - 使用 `Vite 5.4.21`
  - 使用 `@vitejs/plugin-vue 5.2.4`
  - 提供 `npm run dev`、`npm run build`、`npm run preview` 脚本。
- 前端项目通过 Vite dev server 代理 `/api` 到后端 `http://127.0.0.1:5119`。
- WebAPI 静态托管目录调整为 `/src/NetMind.Frontend/dist`，仅托管前端构建产物，不再直接托管前端源码目录。
- 新增根目录 `.gitignore`，忽略 `bin/`、`obj/`、`node_modules/`、`dist/` 和本地工具缓存。
- 新增 `src/NuGet.Config`，清空包源，保证当前无第三方 .NET 包依赖的最小工程可离线 restore/build。

### 验证结果

- 当前机器安装的 .NET SDK 为 `10.0.101`，未发现 `8.0.x` SDK；所有 .NET 项目已明确设置目标框架为 `net8.0`。
- 已执行 `dotnet restore src/NetMind.sln --configfile src/NuGet.Config`，通过。
- 已执行 `dotnet build src/NetMind.sln -c Release --no-restore -v minimal`，通过，结果为 0 警告、0 错误。
- Debug 构建验证时发现 `IIS Express Worker Process (11508)` 正在锁定 WebApi Debug 输出 DLL，因此本次未强制终止该进程，改用 Release 构建验证代码正确性。
- 已执行 `npm install --cache E:\Private\AAW+\NetMind\NetMind-AI\.npm-cache`，通过并生成 `package-lock.json`。
- 已执行 `npm run build`，通过，生成 Vue3/Vite 前端构建产物。
- 已使用 Release 构建产物启动 WebAPI 并验证：
  - `GET http://127.0.0.1:5091/api/system/health` 返回 `200`。
  - `GET http://127.0.0.1:5091/` 返回 `200`，页面包含 `NetMind`。

### 说明

- 本阶段未引入数据库、AI 调用、认证授权、Swagger 或后端第三方 NuGet 包，保持 P0.1 的最小可运行范围。
- npm 安装后报告依赖链存在 2 个 moderate 漏洞；本阶段未执行 `npm audit fix --force`，避免强制升级造成额外变更。
- 由于当前执行环境限制访问用户级 NuGet/npm 缓存目录以及启动 esbuild 子进程，部分 restore/build/npm 验证使用了提升权限执行。

## 2026-04-28 P0.2 落地数据库搭建最小表结构

### 本阶段目标

- 根据 `/AI文档/项目研发说明.md` 中的数据库结构参考，落地 PostgreSQL 最小建表脚本。
- 覆盖最小闭环表：`mind_map`、`node`、`node_relation`、`node_meta`。
- 提供主键、外键、必要索引和 3 组样例数据。

### 完成内容

- 新增 `/AI文档/SQL/Init.sql`。
- 建立四张 PostgreSQL 表：
  - `mind_map`：思维导图主表，包含标题、根节点、创建/更新时间。
  - `node`：节点表，包含所属导图、父节点、标题、内容、同级排序、创建/更新时间。
  - `node_relation`：节点关系表，包含起点、终点、关系类型、权重、所属导图、创建时间。
  - `node_meta`：节点元数据表，使用 `(node_id, key)` 复合主键保存扩展属性。
- 建立主键与外键：
  - `node.map_id -> mind_map.id`
  - `node.parent_id -> node.id`
  - `node_relation.source_id -> node.id`
  - `node_relation.target_id -> node.id`
  - `node_relation.map_id -> mind_map.id`
  - `mind_map.root_node_id -> node.id`
  - `node_meta.node_id -> node.id`
- 建立必要索引：
  - `node.map_id`
  - `node.parent_id`
  - `node(map_id, parent_id, order_no)`
  - `node` 标题/内容全文检索 GIN 表达式索引
  - `node_relation.source_id`
  - `node_relation.target_id`
  - `node_relation.relation_type`
  - `node_relation.map_id`
  - `node_relation(map_id, relation_type)`
  - `node_meta.key`
- 初始化 3 组样例数据：
  - 产品需求知识图谱
  - 技术方案知识图谱
  - 项目计划知识图谱
- 为样例节点补充 3 条 `node_meta` 元数据。

### 脚本幂等与安全性

- 表结构使用 `CREATE TABLE IF NOT EXISTS`。
- 索引使用 `CREATE INDEX IF NOT EXISTS`。
- 循环外键 `mind_map.root_node_id -> node.id` 使用条件 `DO` 块追加，避免重复执行时报错。
- 初始化数据使用固定 ID 与 `ON CONFLICT ... DO UPDATE`，脚本可重复执行。
- 显式执行 `setval`，保证手动插入样例数据后 identity 序列继续可用。
- 样例数据先插入 `mind_map`，再插入 `node`，再回填 `root_node_id`，最后插入 `node_relation` 和 `node_meta`，避免外键插入顺序失败。

### 验证结果

- 已静态检查 `/AI文档/SQL/Init.sql`，确认包含四张表、主键、外键、必要索引和 3 组样例数据。
- 当前机器未安装 `psql` 命令，无法在本机直接连接 PostgreSQL 执行建表验证。

### 说明

- 全文索引使用 PostgreSQL `to_tsvector('simple', ...)` 表达式索引，满足 P0 阶段最小检索能力，后续可根据中文分词方案再升级。

## 2026-04-28 P0.3 支持思维导图与节点增删改查

### 本阶段目标

- 支持思维导图、节点、节点关联的核心增删改查接口。
- 删除统一采用逻辑删除。
- 按人工审核点提供导图、节点、节点关联的成对删除能力。
- 提供 Swagger/OpenAPI 入口和核心集成测试。

### 完成内容

- 新增 DTO：
  - `MindMapDto`、`CreateMindMapRequest`、`UpdateMindMapRequest`
  - `NodeDto`、`CreateNodeRequest`、`UpdateNodeRequest`
  - `NodeRelationDto`、`CreateNodeRelationRequest`、`UpdateNodeRelationRequest`
  - `DeleteResultDto`
- 新增实体模型：
  - `MindMapEntity`
  - `NodeEntity`
  - `NodeRelationEntity`
  - `NodeMetaEntity`
- 新增 Repository/Service/Controller 分层：
  - `IMindMapRepository` / `MindMapRepository`
  - `INodeRepository` / `NodeRepository`
  - `INodeRelationRepository` / `NodeRelationRepository`
  - `IMindMapService` / `MindMapService`
  - `INodeService` / `NodeService`
  - `INodeRelationService` / `NodeRelationService`
  - `MindMapsController`
  - `NodesController`
  - `NodeRelationsController`
- 当前 P0.3 使用内存 Repository 承载接口行为，保留 Repository 边界，后续接入 PostgreSQL 时可替换实现。
- 新增手写 OpenAPI/Swagger 入口：
  - `GET /swagger`
  - `GET /swagger/index.html`
  - `GET /swagger/v1/swagger.json`
- 新增无第三方依赖集成测试项目：
  - `/src/NetMind.IntegrationTests`

### 核心接口清单

- 思维导图：
  - `GET /api/mind-maps`
  - `GET /api/mind-maps/{id}`
  - `POST /api/mind-maps`
  - `PUT /api/mind-maps/{id}`
  - `DELETE /api/mind-maps/{id}`：仅逻辑删除导图本身。
  - `DELETE /api/mind-maps/{id}/cascade`：逻辑删除导图及其节点、节点关联。
- 节点：
  - `GET /api/nodes/by-map/{mapId}`
  - `GET /api/nodes/{id}`
  - `POST /api/nodes`
  - `PUT /api/nodes/{id}`
  - `DELETE /api/nodes/{id}`：逻辑删除节点本身，保留子节点并提升到原父级。
  - `DELETE /api/nodes/{id}/subtree`：逻辑删除节点本身及其子树。
- 节点关联：
  - `GET /api/node-relations/by-map/{mapId}`
  - `GET /api/node-relations/{id}`
  - `POST /api/node-relations`
  - `PUT /api/node-relations/{id}`
  - `DELETE /api/node-relations/{id}`：逻辑删除单条节点关联。
  - `DELETE /api/node-relations/by-node/{nodeId}`：逻辑删除与节点相关的全部关联。

### SQL 变更

- 更新 `/AI文档/SQL/Init.sql`：
  - `mind_map`、`node`、`node_relation` 新增 `is_deleted`、`deleted_at` 字段。
  - 查询索引调整为带 `is_deleted = FALSE` 的部分索引。
- 新增 `/AI文档/SQL/P0.3_AddLogicalDelete.sql`：
  - 为已有 P0.2 数据库安全追加逻辑删除字段。
  - 为未删除数据追加必要部分索引。
  - 脚本使用 `ADD COLUMN IF NOT EXISTS` 和 `CREATE INDEX IF NOT EXISTS`，可重复执行。

### 验证结果

- 已执行 `dotnet restore src/NetMind.sln --configfile src/NuGet.Config`，通过。
- 已执行 `dotnet build src/NetMind.sln --no-restore -v minimal`，通过，结果为 0 警告、0 错误。
- 已执行 `dotnet run --project src/NetMind.IntegrationTests/NetMind.IntegrationTests.csproj --no-build`，通过。
- 已启动 WebAPI 并验证：
  - `GET http://127.0.0.1:5123/swagger/v1/swagger.json` 返回 `200`。
  - `GET http://127.0.0.1:5123/api/mind-maps` 返回 `200`。

### 说明

- 当前环境仍未引入第三方 NuGet 包；Swagger 使用项目内手写 OpenAPI JSON 与轻量 HTML 页，避免破坏离线构建约束。
- 当前接口数据暂存于内存 Repository，进程重启后恢复种子数据；真实 PostgreSQL 持久化建议放入后续阶段实现。

## 2026-04-28 P0.4 前端最小可视化展示

### 本阶段目标

- 在前端提供最小思维导图可视化工作台。
- 对接 P0.3 的导图、节点、节点关联 API。
- 主流程可从页面完成导图选择、节点展示、节点编辑和关联维护。
- 出错时给出明确提示。

### 完成内容

- 重构 `/src/NetMind.Frontend/src/App.vue`：
  - 左侧导图列表，支持刷新、新增、重命名、逻辑删除、级联逻辑删除。
  - 中间节点树列表，按父子层级缩进展示，支持选择节点。
  - 右侧节点表单，支持创建根节点、创建子节点、保存节点、删除本身、删除子树。
  - 右侧关联表单，支持选择目标节点创建关联，并删除已有节点关联。
  - 统一封装 API 请求，后端错误和网络错误会显示在页面顶部。
  - 成功操作会显示明确状态提示。
- 重构 `/src/NetMind.Frontend/src/styles.css`：
  - 三栏工作台布局，移动端收敛为单列。
  - 导图、节点、关联均提供选中态和禁用态。
  - 错误提示和成功提示使用独立样式。
- 更新后端健康状态阶段为 `P0.4`。
- 前端继续通过 Vite dev server 代理 `/api` 到 `http://127.0.0.1:5119`，生产构建产物继续由 WebAPI 静态托管。

### 主流程

- 打开页面后自动请求 `GET /api/mind-maps`。
- 选择导图后并行请求：
  - `GET /api/nodes/by-map/{mapId}`
  - `GET /api/node-relations/by-map/{mapId}`
- 用户可在页面上完成：
  - 新建导图。
  - 新建根节点或子节点。
  - 选择节点并编辑标题、内容、排序。
  - 删除节点本身或删除节点及子树。
  - 为当前节点创建到其他节点的关联。
  - 删除节点关联。

### 验证结果

- 已执行 `npm run build`，通过，生成前端生产构建产物。
- 已执行 `dotnet build src/NetMind.sln -c Release --no-restore -v minimal`，通过，结果为 0 警告、0 错误。
- 已执行 `dotnet run --project src/NetMind.IntegrationTests/NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
- 已使用已批准的本地验证命令启动 WebAPI 并验证：
  - `GET http://127.0.0.1:5123/swagger/v1/swagger.json` 返回 `200`。
  - `GET http://127.0.0.1:5123/api/mind-maps` 返回 `200`。
- Debug 构建仍受当前机器已有 `IIS Express Worker Process` 锁定 WebAPI Debug 输出 DLL 影响，本阶段未强制终止该进程，使用 Release 构建完成验证。

### 说明

- 当前前端以 P0 最小可视化为目标，采用列表式树形展示，没有引入额外前端组件库。
- 当前数据仍来自 P0.3 内存 Repository，刷新页面保留当前进程内数据，重启后恢复种子数据；数据库持久化接入留待后续阶段。

## 2026-04-29 P0.5 前端最小可视化验证最小业务闭环稳定可用

### 本阶段目标

- 修复前端工作台在当前源码中的乱码、字符串破损和模板标签破损问题，保证生产构建可通过。
- 稳定前端最小业务闭环：导图列表、导图创建/重命名/删除、节点创建/编辑/删除、节点关联创建/删除。
- 提供可重复执行的冒烟脚本，并记录不少于 10 条冒烟用例结果。

### 完成内容

- 重写 `/src/NetMind.Frontend/src/App.vue`：
  - 页面阶段标识更新为 `P0.5`。
  - 恢复中文界面文案，修复损坏的按钮标签、占位文案、错误提示和成功提示。
  - 合并导图数据加载流程，避免节点与关联并发加载时互相覆盖 loading/notice 状态。
  - 新增节点表单复位与回填逻辑，刷新、删除、保存后保持或清理选中状态。
  - 关联列表改为优先展示当前选中节点相关关联，并显示节点标题。
  - 为关键控件补充 `data-testid`，方便后续前端自动化验证。
- 更新 `/src/NetMind.Frontend/src/styles.css`，补充小尺寸空状态样式。
- 更新 `/src/NetMind.Repository/Implementations/ProjectStatusRepository.cs`，健康检查阶段从 `P0.4` 更新为 `P0.5`。
- 调整创建接口返回方式：
  - `/src/NetMind.WebApi/Controllers/MindMapsController.cs`
  - `/src/NetMind.WebApi/Controllers/NodesController.cs`
  - `/src/NetMind.WebApi/Controllers/NodeRelationsController.cs`
  - 创建接口改为直接返回 `201 Created` 与标准 `ApiResult` 响应体，避免 `CreatedAtAction` 的 Location 生成链路影响最小闭环验证。
- 新增 `/AI文档/Smoke/P0.5-Smoke.ps1`，覆盖前端静态页面托管、健康检查、导图、节点、关联与级联删除闭环。

### 冒烟用例与结果记录

执行命令：

```powershell
powershell.exe -ExecutionPolicy Bypass -File 'AI文档\Smoke\P0.5-Smoke.ps1' -BaseUrl 'http://127.0.0.1:5124' -StartServer
```

结果：13/13 通过。

| 编号 | 用例 | 结果 | 记录 |
| --- | --- | --- | --- |
| 01 | 健康检查返回 P0.5 | PASS | `phase=P0.5` |
| 02 | 前端首页可由 WebAPI 静态托管访问 | PASS | 首页内容包含 `NetMind` |
| 03 | 查询种子导图列表 | PASS | 返回 3 条种子导图 |
| 04 | 创建导图 | PASS | 返回新导图 id |
| 05 | 重命名导图 | PASS | 标题更新为 `P0.5 smoke map updated` |
| 06 | 创建根节点 | PASS | 节点归属到新导图 |
| 07 | 创建子节点 | PASS | 子节点 parentId 指向根节点 |
| 08 | 按导图查询节点并保持层级 | PASS | 返回 2 个节点且父子关系正确 |
| 09 | 更新节点 | PASS | 标题和排序保存成功 |
| 10 | 创建节点关联 | PASS | 关联类型为 `depends_on` |
| 11 | 按导图查询节点关联 | PASS | 返回 1 条关联 |
| 12 | 删除节点关联 | PASS | 逻辑删除影响 1 条 |
| 13 | 级联删除导图 | PASS | 导图、2 个节点共影响 3 条 |

### 其他验证结果

- 已执行 `npm run build`，通过。
- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。

### 已知问题清单（人工审核点）

- 当前 Repository 仍为内存实现，服务进程重启后恢复种子数据，真实 PostgreSQL 持久化仍待后续阶段接入。
- 前端仍为最小列表式可视化验证，尚未提供画布式思维导图布局、拖拽编辑、节点连线图形化展示。
- npm 安装后仍报告 2 个 moderate 级别依赖漏洞；本阶段未执行 `npm audit fix --force`，避免强制升级引入额外变更。

## 2026-04-29 P1.0 导入导出整个思维导图接口及前端基础交互

### 本阶段目标

- 设计完整思维导图导入导出结构体。
- 新增结构体导入、结构体返回、文件导入、文件导出、模板下载接口。
- 在前端提供基础导入导出交互。
- 补充测试与冒烟验证记录。

### 导入导出结构体设计（人工审核点）

P1.0 使用稳定传输结构 `MindMapTransferDto`，导入导出文件均为 JSON：

```json
{
  "schemaVersion": "netmind.mindmap.v1",
  "title": "Imported mind map",
  "nodes": [
    {
      "clientId": "root",
      "parentClientId": null,
      "title": "Root topic",
      "content": "Describe the central topic.",
      "orderNo": 1
    }
  ],
  "relations": [
    {
      "sourceClientId": "root",
      "targetClientId": "child-1",
      "relationType": "relates_to",
      "weight": 1
    }
  ]
}
```

设计说明：

- `schemaVersion` 固定为 `netmind.mindmap.v1`，后续结构升级时可兼容处理。
- 导入文件不使用数据库 id，而使用 `clientId`、`parentClientId`、`sourceClientId`、`targetClientId` 表达树结构和关联关系。
- 导入时系统重新生成真实 `mapId/nodeId/relationId`，并返回 `NodeIdMap`，避免跨环境导入时 id 冲突。
- 导出结构体中 `clientId` 使用 `node-{id}` 形式，只作为导出文件内的稳定引用键。
- 服务端校验内容包括：版本、导图标题、节点 clientId 非空且唯一、父节点存在、关联端点存在、关联端点不同、关系类型非空、权重非负、父子循环。

### 新增/调整内容

- 新增 DTO：`/src/NetMind.Models/Dtos/MindMapTransferDtos.cs`
  - `MindMapTransferDto`
  - `MindMapTransferNodeDto`
  - `MindMapTransferRelationDto`
  - `ImportMindMapRequest`
  - `MindMapStructureDto`
  - `ImportedMindMapDto`
- 新增服务：
  - `/src/NetMind.Services/Interfaces/IMindMapTransferService.cs`
  - `/src/NetMind.Services/Implementations/MindMapTransferService.cs`
- 新增控制器：`/src/NetMind.WebApi/Controllers/MindMapTransferController.cs`
- 更新依赖注入：`/src/NetMind.WebApi/Program.cs`
- 更新 Swagger 手写文档：`/src/NetMind.WebApi/Swagger/SwaggerDocumentFactory.cs`
- 健康检查阶段更新为 `P1.0`。
- 前端 `/src/NetMind.Frontend/src/App.vue` 新增 Import / Export 面板：
  - 导出当前导图结构到文本框。
  - 下载当前导图 JSON 文件。
  - 下载导入模板。
  - 粘贴结构体直接导入。
  - 选择 JSON 文件导入。
  - 可选导入标题覆盖。
- 前端 `/src/NetMind.Frontend/src/styles.css` 补充导入导出面板布局。

### 新增接口清单

- `GET /api/mind-map-transfer/{mapId}/structure`：结构体返回完整导图。
- `GET /api/mind-map-transfer/{mapId}/file`：导出完整导图 JSON 文件。
- `POST /api/mind-map-transfer/structure`：通过结构体直接导入完整导图。
- `POST /api/mind-map-transfer/file`：通过上传 JSON 文件导入完整导图。
- `GET /api/mind-map-transfer/template`：下载 JSON 导入模板。

### 单元测试/集成验证

已更新 `/src/NetMind.IntegrationTests/Program.cs`，覆盖：

- 模板结构版本与节点样例。
- 结构体导入后生成导图、节点、关联。
- 导入返回 `NodeIdMap`。
- 导出结构体包含节点、关联并保留层级引用。
- 非法关联端点导入失败。

执行结果：

```powershell
dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build
```

结果：通过，输出 `NetMind integration tests passed.`。

### 冒烟验证

新增 `/AI文档/Smoke/P1.0-Smoke.ps1`，执行命令：

```powershell
powershell.exe -ExecutionPolicy Bypass -File 'AI文档\Smoke\P1.0-Smoke.ps1' -StartServer
```

结果：6/6 通过。

| 编号 | 用例 | 结果 | 记录 |
| --- | --- | --- | --- |
| 01 | 健康检查返回 P1.0 | PASS | `phase=P1.0` |
| 02 | 模板下载返回传输结构 | PASS | `schemaVersion=netmind.mindmap.v1`，模板含 2 个节点 |
| 03 | 结构体导入创建导图 | PASS | 返回新导图 id |
| 04 | 结构体导出返回完整导图 | PASS | 返回 2 个节点和 1 条关联 |
| 05 | 文件导出下载 JSON | PASS | 文件结构版本正确，含 2 个节点 |
| 06 | 文件导入创建导图 | PASS | 标题覆盖为 `P1 smoke file import` |

### 构建验证

- 已执行 `npm run build`，通过。
- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
- 已执行 P1.0 冒烟脚本，6/6 通过。

### 已知问题

- 当前导入导出仍基于内存 Repository，服务重启后导入结果不会持久化到 PostgreSQL。
- JSON 模板和导出文件暂未包含 `node_meta`，因为当前业务接口尚未暴露元数据维护能力。
- 前端基础交互以文本框和文件选择为主，暂未提供可视化导入预览、差异对比或冲突处理。
- npm 安装依赖时仍报告 2 个 moderate 级别漏洞；本阶段未执行强制升级。

### 提交要求建议

- 建议分支名：`codex/p1-import-export`
- 建议 commit：`feat: add P1 mind map import export flow`
- PR 说明：
  - 新增完整导图传输结构 `netmind.mindmap.v1`。
  - 新增结构导入/导出、文件导入/导出、模板下载接口。
  - 前端新增 Import / Export 基础交互。
  - 增加导入导出集成验证和 P1.0 冒烟脚本。
  - 验证：前端构建、Release 后端构建、集成测试、P1.0 冒烟均通过。

## 2026-04-29 P1.1 AI 清洗设计与实现

### 本阶段目标

- 提供 AI 配置占位和模型列表能力。
- 新增 AI 清洗接口，将自然语言扩充并转化为标准导图结构体。
- 前端支持输入自然语言并输出 `netmind.mindmap.v1` 标准结构体。
- 新增 AI 大模型要求及配置说明文档。

### AI 大模型要求及配置说明（人工审核点）

已新增 `/AI文档/AI大模型配置说明.md`。

P1.1 不接入真实外部大模型，采用占位配置和本地确定性清洗规则，保证接口、前端流程、结构校验和后续替换点先稳定落地。

当前模型列表由 `AiCleanService` 写死维护，并通过 `GET /api/ai/models` 返回：

| id | 名称 | provider | endpoint | 状态 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `local-deepseek-placeholder` | DeepSeek Local Placeholder | `local` | `placeholder://local/deepseek` | `placeholder` | P1.1 默认模型，列表第一个，使用本地规则模拟文本清洗。 |
| `cloud-model-placeholder` | Cloud Model Placeholder | `cloud` | `placeholder://cloud/api` | `disabled` | 预留给后续云模型 API。 |

默认选择规则：

- 前端启动调用 `GET /api/ai/models`。
- 列表第一个模型为默认模型。
- `POST /api/ai/clean` 未传 `modelId` 时使用第一个模型。
- 传入未知 `modelId` 时回退到第一个模型。

后续接入真实模型时，建议保持 `IAiCleanService` 接口不变，仅替换服务实现；外部 API Key、Endpoint、超时、重试等应迁移到配置文件或环境变量。

### 新增/调整内容

- 新增 DTO：`/src/NetMind.Models/Dtos/AiDtos.cs`
  - `AiModelOptionDto`
  - `AiCleanRequest`
  - `AiCleanResultDto`
- 新增服务：
  - `/src/NetMind.Services/Interfaces/IAiCleanService.cs`
  - `/src/NetMind.Services/Implementations/AiCleanService.cs`
- 新增控制器：`/src/NetMind.WebApi/Controllers/AiController.cs`
- 更新依赖注入：`/src/NetMind.WebApi/Program.cs`
- 更新 Swagger 手写文档：`/src/NetMind.WebApi/Swagger/SwaggerDocumentFactory.cs`
- 健康检查阶段更新为 `P1.1`。
- 前端 `/src/NetMind.Frontend/src/App.vue` 新增 AI Clean 面板：
  - 自动加载模型列表。
  - 默认选中第一个模型。
  - 支持自然语言输入。
  - 点击清洗后输出标准 `MindMapTransferDto` JSON 到导入导出文本框。
  - 输出结构体可继续使用 P1.0 的结构导入能力。
- 前端 `/src/NetMind.Frontend/src/styles.css` 复用导入导出面板布局样式。

### 新增接口清单

- `GET /api/ai/models`：返回 AI 模型配置占位列表。
- `POST /api/ai/clean`：将自然语言清洗、扩充并转化为 `netmind.mindmap.v1` 标准结构体。

### AI 清洗规则

P1.1 本地清洗规则：

- 输入自然语言按换行和项目符号拆分。
- 第一条有效文本作为导图根节点标题。
- 后续条目扩充为根节点下的子节点。
- 如果只有一段文本，则按句号、问号、感叹号、分号拆分为多个主题项。
- 输出固定为 `netmind.mindmap.v1`。
- 自动生成 `root -> topic-*` 的 `expands_to` 关联。
- 输出结果继续由 P1.0 的导入服务进行结构校验和导入。

### 单元测试/集成验证

已更新 `/src/NetMind.IntegrationTests/Program.cs`，覆盖：

- AI 模型占位列表可返回。
- 第一个模型为默认 `local-deepseek-placeholder`。
- AI 清洗输出 `netmind.mindmap.v1` 标准结构体。
- 自然语言被扩充为节点和关联。
- AI 清洗结果可直接通过 P1.0 导入服务导入。

执行结果：

```powershell
dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build
```

结果：通过，输出 `NetMind integration tests passed.`。

### 冒烟验证

新增 `/AI文档/Smoke/P1.1-Smoke.ps1`，执行命令：

```powershell
powershell.exe -ExecutionPolicy Bypass -File 'AI文档\Smoke\P1.1-Smoke.ps1' -StartServer
```

结果：6/6 通过。

| 编号 | 用例 | 结果 | 记录 |
| --- | --- | --- | --- |
| 01 | 健康检查返回 P1.1 | PASS | `phase=P1.1` |
| 02 | AI 模型列表返回默认模型 | PASS | 默认模型 `local-deepseek-placeholder` |
| 03 | AI 清洗返回标准结构版本 | PASS | `schemaVersion=netmind.mindmap.v1` |
| 04 | AI 清洗扩充节点 | PASS | 返回 4 个节点 |
| 05 | AI 清洗创建关联 | PASS | 返回 3 条关联 |
| 06 | 清洗结构可直接导入 | PASS | 返回新导图 id |

### 构建验证

- 已执行 `npm run build`，通过。
- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
- 已执行 P1.1 冒烟脚本，6/6 通过。

### 已知问题

- P1.1 尚未接入真实大模型 API，当前为本地确定性规则模拟清洗。
- 当前模型配置写死在服务代码中，真实接入时应迁移到配置文件或环境变量。
- 清洗规则只做基础拆分和扩充，无法理解复杂业务语义。
- 前端输出结构体后仍需用户人工确认再导入，暂未提供差异预览或 AI 二次修正。
- npm 安装依赖时仍报告 2 个 moderate 级别漏洞；本阶段未执行强制升级。

### 提交要求建议

- 建议分支名：`codex/p1-ai-cleaning`
- 建议 commit：`feat: add P1 AI cleaning placeholder flow`
- PR 说明：
  - 新增 AI 模型占位配置说明和模型列表接口。
  - 新增自然语言清洗接口，输出 `netmind.mindmap.v1` 标准结构体。
  - 前端新增 AI Clean 面板，支持自然语言输入并输出标准结构体。
  - 增加集成验证和 P1.1 冒烟脚本。
  - 验证：前端构建、Release 后端构建、集成测试、P1.1 冒烟均通过。
