### 导入导出结构体设计（人工审核点）

P1.0 使用稳定传输结构 `MindMapTransferDto`，导入导出文件均为 JSON：

```json
{
  "schemaVersion": "netmind.mindmap.v1",
  "title": "Imported mind map",
  "nodes": [
    {
      "clientId": "root",
      "parentClientId": null,
      "title": "Root topic",
      "content": "Describe the central topic.",
      "orderNo": 1
    }
  ],
  "relations": [
    {
      "sourceClientId": "root",
      "targetClientId": "child-1",
      "relationType": "relates_to",
      "weight": 1
    }
  ]
}
```

设计说明：

- `schemaVersion` 固定为 `netmind.mindmap.v1`，后续结构升级时可兼容处理。
- 导入文件不使用数据库 id，而使用 `clientId`、`parentClientId`、`sourceClientId`、`targetClientId` 表达树结构和关联关系。
- 导入时系统重新生成真实 `mapId/nodeId/relationId`，并返回 `NodeIdMap`，避免跨环境导入时 id 冲突。
- 导出结构体中 `clientId` 使用 `node-{id}` 形式，只作为导出文件内的稳定引用键。
- 服务端校验内容包括：版本、导图标题、节点 clientId 非空且唯一、父节点存在、关联端点存在、关联端点不同、关系类型非空、权重非负、父子循环。

### 新增/调整内容

- 新增 DTO：`/src/NetMind.Models/Dtos/MindMapTransferDtos.cs`
  - `MindMapTransferDto`
  - `MindMapTransferNodeDto`
  - `MindMapTransferRelationDto`
  - `ImportMindMapRequest`
  - `MindMapStructureDto`
  - `ImportedMindMapDto`
- 新增服务：
  - `/src/NetMind.Services/Interfaces/IMindMapTransferService.cs`
  - `/src/NetMind.Services/Implementations/MindMapTransferService.cs`
- 新增控制器：`/src/NetMind.WebApi/Controllers/MindMapTransferController.cs`
- 更新依赖注入：`/src/NetMind.WebApi/Program.cs`
- 更新 Swagger 手写文档：`/src/NetMind.WebApi/Swagger/SwaggerDocumentFactory.cs`
- 健康检查阶段更新为 `P1.0`。
- 前端 `/src/NetMind.Frontend/src/App.vue` 新增 Import / Export 面板：
  - 导出当前导图结构到文本框。
  - 下载当前导图 JSON 文件。
  - 下载导入模板。
  - 粘贴结构体直接导入。
  - 选择 JSON 文件导入。
  - 可选导入标题覆盖。
- 前端 `/src/NetMind.Frontend/src/styles.css` 补充导入导出面板布局。

### 新增接口清单

- `GET /api/mind-map-transfer/{mapId}/structure`：结构体返回完整导图。
- `GET /api/mind-map-transfer/{mapId}/file`：导出完整导图 JSON 文件。
- `POST /api/mind-map-transfer/structure`：通过结构体直接导入完整导图。
- `POST /api/mind-map-transfer/file`：通过上传 JSON 文件导入完整导图。
- `GET /api/mind-map-transfer/template`：下载 JSON 导入模板。

### 单元测试/集成验证

已更新 `/src/NetMind.IntegrationTests/Program.cs`，覆盖：

- 模板结构版本与节点样例。
- 结构体导入后生成导图、节点、关联。
- 导入返回 `NodeIdMap`。
- 导出结构体包含节点、关联并保留层级引用。
- 非法关联端点导入失败。

执行结果：

```powershell
dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build
```

结果：通过，输出 `NetMind integration tests passed.`。

### 冒烟验证

新增 `/AI文档/Smoke/P1.0-Smoke.ps1`，执行命令：

```powershell
powershell.exe -ExecutionPolicy Bypass -File 'AI文档\Smoke\P1.0-Smoke.ps1' -StartServer
```

结果：6/6 通过。

| 编号 | 用例 | 结果 | 记录 |
| --- | --- | --- | --- |
| 01 | 健康检查返回 P1.0 | PASS | `phase=P1.0` |
| 02 | 模板下载返回传输结构 | PASS | `schemaVersion=netmind.mindmap.v1`，模板含 2 个节点 |
| 03 | 结构体导入创建导图 | PASS | 返回新导图 id |
| 04 | 结构体导出返回完整导图 | PASS | 返回 2 个节点和 1 条关联 |
| 05 | 文件导出下载 JSON | PASS | 文件结构版本正确，含 2 个节点 |
| 06 | 文件导入创建导图 | PASS | 标题覆盖为 `P1 smoke file import` |

### 构建验证

- 已执行 `npm run build`，通过。
- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
- 已执行 P1.0 冒烟脚本，6/6 通过。

### 已知问题

- 当前导入导出仍基于内存 Repository，服务重启后导入结果不会持久化到 PostgreSQL。
- JSON 模板和导出文件暂未包含 `node_meta`，因为当前业务接口尚未暴露元数据维护能力。
- 前端基础交互以文本框和文件选择为主，暂未提供可视化导入预览、差异对比或冲突处理。
- npm 安装依赖时仍报告 2 个 moderate 级别漏洞；本阶段未执行强制升级。

### 提交要求建议

- 建议分支名：`codex/p1-import-export`
- 建议 commit：`feat: add P1 mind map import export flow`
- PR 说明：
  - 新增完整导图传输结构 `netmind.mindmap.v1`。
  - 新增结构导入/导出、文件导入/导出、模板下载接口。
  - 前端新增 Import / Export 基础交互。
  - 增加导入导出集成验证和 P1.0 冒烟脚本。
  - 验证：前端构建、Release 后端构建、集成测试、P1.0 冒烟均通过。

## 2026-04-29 P1.1 AI 清洗设计与实现

### 本阶段目标

- 提供 AI 配置占位和模型列表能力。
- 新增 AI 清洗接口，将自然语言扩充并转化为标准导图结构体。
- 前端支持输入自然语言并输出 `netmind.mindmap.v1` 标准结构体。
- 新增 AI 大模型要求及配置说明文档。

### AI 大模型要求及配置说明（人工审核点）

已新增 `/AI文档/AI大模型配置说明.md`。

P1.1 不接入真实外部大模型，采用占位配置和本地确定性清洗规则，保证接口、前端流程、结构校验和后续替换点先稳定落地。

当前模型列表由 `AiCleanService` 写死维护，并通过 `GET /api/ai/models` 返回：

| id | 名称 | provider | endpoint | 状态 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `local-deepseek-placeholder` | DeepSeek Local Placeholder | `local` | `placeholder://local/deepseek` | `placeholder` | P1.1 默认模型，列表第一个，使用本地规则模拟文本清洗。 |
| `cloud-model-placeholder` | Cloud Model Placeholder | `cloud` | `placeholder://cloud/api` | `disabled` | 预留给后续云模型 API。 |

默认选择规则：

- 前端启动调用 `GET /api/ai/models`。
- 列表第一个模型为默认模型。
- `POST /api/ai/clean` 未传 `modelId` 时使用第一个模型。
- 传入未知 `modelId` 时回退到第一个模型。

后续接入真实模型时，建议保持 `IAiCleanService` 接口不变，仅替换服务实现；外部 API Key、Endpoint、超时、重试等应迁移到配置文件或环境变量。

### 新增/调整内容

- 新增 DTO：`/src/NetMind.Models/Dtos/AiDtos.cs`
  - `AiModelOptionDto`
  - `AiCleanRequest`
  - `AiCleanResultDto`
- 新增服务：
  - `/src/NetMind.Services/Interfaces/IAiCleanService.cs`
  - `/src/NetMind.Services/Implementations/AiCleanService.cs`
- 新增控制器：`/src/NetMind.WebApi/Controllers/AiController.cs`
- 更新依赖注入：`/src/NetMind.WebApi/Program.cs`
- 更新 Swagger 手写文档：`/src/NetMind.WebApi/Swagger/SwaggerDocumentFactory.cs`
- 健康检查阶段更新为 `P1.1`。
- 前端 `/src/NetMind.Frontend/src/App.vue` 新增 AI Clean 面板：
  - 自动加载模型列表。
  - 默认选中第一个模型。
  - 支持自然语言输入。
  - 点击清洗后输出标准 `MindMapTransferDto` JSON 到导入导出文本框。
  - 输出结构体可继续使用 P1.0 的结构导入能力。
- 前端 `/src/NetMind.Frontend/src/styles.css` 复用导入导出面板布局样式。

### 新增接口清单

- `GET /api/ai/models`：返回 AI 模型配置占位列表。
- `POST /api/ai/clean`：将自然语言清洗、扩充并转化为 `netmind.mindmap.v1` 标准结构体。

### AI 清洗规则

P1.1 本地清洗规则：

- 输入自然语言按换行和项目符号拆分。
- 第一条有效文本作为导图根节点标题。
- 后续条目扩充为根节点下的子节点。
- 如果只有一段文本，则按句号、问号、感叹号、分号拆分为多个主题项。
- 输出固定为 `netmind.mindmap.v1`。
- 自动生成 `root -> topic-*` 的 `expands_to` 关联。
- 输出结果继续由 P1.0 的导入服务进行结构校验和导入。

### 单元测试/集成验证

已更新 `/src/NetMind.IntegrationTests/Program.cs`，覆盖：

- AI 模型占位列表可返回。
- 第一个模型为默认 `local-deepseek-placeholder`。
- AI 清洗输出 `netmind.mindmap.v1` 标准结构体。
- 自然语言被扩充为节点和关联。
- AI 清洗结果可直接通过 P1.0 导入服务导入。

执行结果：

```powershell
dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build
```

结果：通过，输出 `NetMind integration tests passed.`。

### 冒烟验证

新增 `/AI文档/Smoke/P1.1-Smoke.ps1`，执行命令：

```powershell
powershell.exe -ExecutionPolicy Bypass -File 'AI文档\Smoke\P1.1-Smoke.ps1' -StartServer
```

结果：6/6 通过。

| 编号 | 用例 | 结果 | 记录 |
| --- | --- | --- | --- |
| 01 | 健康检查返回 P1.1 | PASS | `phase=P1.1` |
| 02 | AI 模型列表返回默认模型 | PASS | 默认模型 `local-deepseek-placeholder` |
| 03 | AI 清洗返回标准结构版本 | PASS | `schemaVersion=netmind.mindmap.v1` |
| 04 | AI 清洗扩充节点 | PASS | 返回 4 个节点 |
| 05 | AI 清洗创建关联 | PASS | 返回 3 条关联 |
| 06 | 清洗结构可直接导入 | PASS | 返回新导图 id |

### 构建验证

- 已执行 `npm run build`，通过。
- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
- 已执行 P1.1 冒烟脚本，6/6 通过。

### 已知问题

- P1.1 尚未接入真实大模型 API，当前为本地确定性规则模拟清洗。
- 当前模型配置写死在服务代码中，真实接入时应迁移到配置文件或环境变量。
- 清洗规则只做基础拆分和扩充，无法理解复杂业务语义。
- 前端输出结构体后仍需用户人工确认再导入，暂未提供差异预览或 AI 二次修正。
- npm 安装依赖时仍报告 2 个 moderate 级别漏洞；本阶段未执行强制升级。

### 提交要求建议

- 建议分支名：`codex/p1-ai-cleaning`
- 建议 commit：`feat: add P1 AI cleaning placeholder flow`
- PR 说明：
  - 新增 AI 模型占位配置说明和模型列表接口。
  - 新增自然语言清洗接口，输出 `netmind.mindmap.v1` 标准结构体。
  - 前端新增 AI Clean 面板，支持自然语言输入并输出标准结构体。
  - 增加集成验证和 P1.1 冒烟脚本。
  - 验证：前端构建、Release 后端构建、集成测试、P1.1 冒烟均通过。

## 2026-04-29 P1.2 数据库配置与 AI 清洗方法优化

### 本阶段目标

- 完成 PostgreSQL 数据库连接配置，接口数据只从数据库读取和写入。
- 移除本地内存种子数据与本地模拟清洗逻辑。
- AI 清洗改为配置驱动，优先使用 DeepSeek 云模型，本地 Ollama 作为备用模型。
- 数据库连接和 AI 大模型配置均放入配置文件。

### 完成内容

- 后端 Repository 从内存实现替换为 PostgreSQL 实现：
  - `/src/NetMind.Repository/Implementations/MindMapRepository.cs`
  - `/src/NetMind.Repository/Implementations/NodeRepository.cs`
  - `/src/NetMind.Repository/Implementations/NodeRelationRepository.cs`
- 新增数据库连接工厂：
  - `/src/NetMind.Repository/Implementations/PostgresConnectionFactory.cs`
- 删除本地内存死数据代码：
  - `/src/NetMind.Repository/Implementations/InMemoryMindMapStore.cs`
- 新增 `Npgsql 9.0.3` 依赖，Repository 层通过 PostgreSQL 执行导图、节点、节点关联 CRUD 与逻辑删除。
- 更新 `/src/NetMind.WebApi/appsettings.json` 与 `/src/NetMind.WebApi/appsettings.Development.json`：
  - `ConnectionStrings:Postgres`
  - `AiClean:Models`
- AI 清洗服务改为真实 HTTP 调用：
  - 默认模型：`deepseek-cloud`
  - 本地备用：`ollama-local`
  - 未指定 `modelId` 时 DeepSeek 失败后自动尝试 Ollama。
  - 指定 `modelId` 时只调用指定模型，不再静默回退。
- 更新 `/AI文档/AI大模型配置说明.md`，补充 DeepSeek、Ollama、环境变量和数据库连接配置。
- 健康检查阶段更新为 `P1.2`。
- 前端阶段标识更新为 `P1.2`。
- 新增 `/AI文档/Smoke/P1.2-Smoke.ps1`，用于验证健康检查、AI 模型配置和数据库接口连通。

### 配置说明

数据库配置位于配置文件：

```json
"ConnectionStrings": {
  "Postgres": "Host=127.0.0.1;Port=5432;Database=netmind;Username=postgres;Password=postgres"
}
```

AI 配置位于配置文件：

```json
"AiClean": {
  "Models": [
    {
      "Id": "deepseek-cloud",
      "Provider": "deepseek",
      "Endpoint": "https://api.deepseek.com/chat/completions",
      "Model": "deepseek-chat",
      "Enabled": true,
      "IsDefault": true,
      "ApiKeyEnvironmentVariable": "DEEPSEEK_API_KEY"
    },
    {
      "Id": "ollama-local",
      "Provider": "ollama",
      "Endpoint": "http://127.0.0.1:11434/api/chat",
      "Model": "deepseek-r1:7b",
      "Enabled": true
    }
  ]
}
```

### 接口更新

- `GET /api/ai/models`：返回配置文件中的 DeepSeek 与 Ollama 模型，不再返回 placeholder。
- `POST /api/ai/clean`：调用真实 DeepSeek/Ollama 接口并要求返回 `netmind.mindmap.v1` JSON，不再使用本地规则模拟转化。
- 导图、节点、节点关联接口继续保持原路径，但数据源改为 PostgreSQL。

### 验证结果

- 已执行 `dotnet restore src\NetMind.sln --configfile src\NuGet.Config`，通过。
- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
  - 当前环境未设置 `NETMIND_TEST_POSTGRES_CONNECTION`，数据库写入集成测试按脚本逻辑跳过。
- 已执行 `npm run build`，通过。
- 尝试启动 WebAPI 做本地接口冒烟时受当前沙箱权限限制，`Start-Process` 启动验证未执行成功；代码构建与配置测试已通过。

### 已知问题

- 当前执行环境未提供可连接的 PostgreSQL 实例，因此未完成真实数据库连通冒烟；配置好 PostgreSQL 并执行 `/AI文档/SQL/Init.sql` 后，可运行 `/AI文档/Smoke/P1.2-Smoke.ps1`。
- 当前执行环境未配置 `DEEPSEEK_API_KEY`，也未确认本机 Ollama 模型存在，因此未执行真实 AI 清洗网络调用。
- `/AI文档/SQL/Init.sql` 仍包含数据库初始化样例数据；本阶段移除的是后端本地内存死数据代码。

### 追加修复记录

- 修复前端报错 `Unexpected token 'N', "Npgsql.Pos"... is not valid JSON`：
  - 后端新增全局异常 JSON 包装，未捕获异常统一返回标准 `ApiResult`。
  - 数据库异常返回 `Database request failed: ...`，便于定位连接、表结构或权限问题。
  - 前端 API 封装增加非 JSON 响应兜底，不再因 `JSON.parse` 掩盖真实错误。
- 修复数据库连接配置覆盖问题：
  - `appsettings.json` 与 `appsettings.Development.json` 的 PostgreSQL 连接均统一为 `damp2/postgres/admin`。
  - 避免 Development 环境用 `appsettings.Development.json` 覆盖主配置后仍使用旧密码。
  - 同步修正 Ollama 地址为 `/api/chat`，符合当前 AI 清洗服务调用格式。
- 追加验证：
  - `dotnet build src\NetMind.sln -c Release --no-restore -v minimal` 通过，0 警告，0 错误。
  - `npm run build` 通过。
  - `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build` 通过；当前未设置数据库连接变量，数据库写入测试跳过。

### 追加修复记录 2

- 将 AI 提示词从 `/src/NetMind.Services/Implementations/AiCleanService.cs` 迁移到配置文件：
  - 新增 `AiClean:Prompt:SystemPromptLines`
  - 新增 `AiClean:Prompt:UserPromptTemplateLines`
- 服务层不再硬编码主提示词，只保留：
  - 配置读取
  - `{{schemaVersion}}`、`{{naturalLanguage}}` 占位符替换
  - 模型请求与返回结构校验
- 更新 `/AI文档/AI大模型配置说明.md`，新增提示词占位符规范与提示词编写规范：
  - 输出结构约束
  - System Prompt 与 User Prompt 职责划分
  - 按行配置的维护方式
  - 提示词变更时必须同步更新的文档与配置文件

### 提交要求建议

- 建议分支名：`codex/p1-ai-cleaning`
- 建议 commit：`feat: configure postgres and real AI cleaning`
- PR 说明：
  - 将导图、节点、节点关联 Repository 替换为 PostgreSQL 实现。
  - 移除内存种子数据和本地模拟 AI 清洗。
  - AI 清洗改为配置驱动，默认 DeepSeek 云模型，Ollama 本地备用。
  - 数据库连接和 AI 模型均迁移到 `appsettings`。
  - 更新 AI 配置文档和 P1.2 冒烟脚本。
  - 验证：restore、Release 构建、集成测试、前端构建通过；真实 DB/AI 冒烟需在配置好 PostgreSQL 与模型凭据后执行。
### 追加修复记录 3

- 前后端监听主机默认改为同时支持 IPv4 和 IPv6：
  - 前端 `vite dev` 与 `vite preview` 改为 `--host`，启动时监听所有本地地址。
  - `vite.config.js` 显式配置 `server.host` 与 `preview.host` 为 `true`，避免后续回落到仅 IPv4。
  - WebAPI 默认 `UseUrls` 改为 `http://0.0.0.0:5119` 和 `http://[::]:5119`，同时监听 IPv4 / IPv6。
  - Development 环境下前端存活探测同时检查 `127.0.0.1` 与 `::1`，避免重复启动。
  - `launchSettings.json` 的 Project profile 同步更新为双栈监听地址，确保本地启动行为一致。

## 2026-04-30 P1.3 AI 清洗功能完善

### 本阶段目标

- 新增带用户上下文的 AI 需求结构化拆解能力。
- 上下文较长时先要求 AI 压缩上下文，再进入需求拆解。
- 检查并补强完整思维导图导入接口结构校验。
- 优化 AI 请求等待体验：等待返回时清空输出栏并显示状态提示。

### 完成内容

- 后端新增 DTO：
  - `AiRequirementStructureRequest`
  - `AiRequirementStructureResultDto`
- 后端新增服务方法：
  - `IAiCleanService.StructureRequirementAsync`
  - `AiCleanService.StructureRequirementAsync`
- 新增接口：
  - `POST /api/ai/requirements/structure`
  - 入参：`requirement`、`context`、`modelId`
  - 出参：标准 `netmind.mindmap.v1` 结构体、实际使用模型、最终 Prompt、上下文摘要、是否压缩上下文。
- 新增上下文压缩流程：
  - 配置项：`AiClean:Prompt:ContextCompressionThreshold`
  - 当 `context` 长度超过阈值时，先使用同一模型执行 `ContextCompressionPromptTemplateLines`。
  - 压缩后的上下文再进入 `RequirementPromptTemplateLines`，避免长上下文直接挤占需求拆解窗口。
- AI Prompt 配置更新：
  - 新增 `RequirementPromptTemplateLines`
  - 新增 `ContextCompressionPromptTemplateLines`
  - 新增 `ContextCompressionThreshold`
  - 已同步更新 `/AI文档/项目/AI大模型配置说明.md`。
- 导入完整思维导图接口校验检查结果：
  - 原 `MindMapTransferService.ValidateTransfer` 已校验 schema、标题、clientId 唯一、父节点存在、关联端点存在、关联端点不同、关系类型、权重。
  - 本阶段补强节点数量必须大于 0。
  - 本阶段补强关联 source/target 不能为空，避免空端点导致非业务化异常。
- Swagger 手写文档新增新接口路径。
- 系统阶段标识更新为 `P1.3`。
- 前端 AI 面板新增：
  - 需求对话入口，不再提供独立的“描述不成熟需求”输入框。
  - 需求对话弹窗，显示本次用户与 AI 的对话记录。
  - `New chat` 按钮，清空本次对话记录和上下文。
  - 移除弹窗内重复的 `Structure requirement` 动作，避免与发送澄清对话重复。
  - `生成结构体` 按钮，将本次对话上下文直接复用原 `/api/ai/clean` 生成标准结构体。
  - AI 状态提示。
- 后端新增对话接口：
  - `POST /api/ai/context-chat`
  - 输入最新消息与程序组装的上下文。
  - 输出本轮 AI 回复，长上下文仍按阈值先压缩。
  - 对话 Prompt 改为需求澄清专用：不套用固定维度清单，而是根据需求类型动态选择目标用户、场景、工作流、输入输出、数据来源、约束、验收标准、边界情况等追问方向，每轮只问 1 到 3 个关键问题。
- 前端等待体验优化：
  - `POST /api/ai/clean` 请求发出前清空结构输出栏。
  - `POST /api/ai/requirements/structure` 请求发出前清空结构输出栏。
  - `生成结构体` 请求发出前清空结构输出栏。
  - 请求等待期间显示 AI 处理中状态。
  - 新接口返回时提示是否发生了长上下文压缩。

### 人工审核点

- AI 提示词：
  - `RequirementPromptTemplateLines` 负责不成熟需求的产品化拆解，覆盖角色、目标、流程、数据对象、业务规则、风险、开放问题、验收标准和实现说明。
  - `ContextCompressionPromptTemplateLines` 负责保留上下文中的领域事实、决策、约束、角色、数据对象、术语、风险和待确认问题。
- 上下文方法设计与实现：
  - 压缩阈值配置化，默认 `4000` 字符。
  - 未指定模型时仍按默认模型优先、失败后备用模型的策略执行。
  - 指定模型时压缩和结构化均只使用该模型。

### 接口更新

- `POST /api/ai/requirements/structure`
- `POST /api/ai/context-chat`

请求示例：

```json
{
  "requirement": "我想做一个能帮用户整理想法的功能，但还没想清楚流程",
  "context": "历史讨论、目标用户、业务限制或聊天上下文",
  "modelId": "deepseek-cloud"
}
```

对话接口请求示例：

```json
{
  "message": "这个需求还缺哪些关键问题？",
  "context": "User: 我想做一个需求整理功能\nAI: 可以先确认用户角色和输出格式。",
  "modelId": "deepseek-cloud"
}
```

响应核心字段：

```json
{
  "selectedModel": {},
  "prompt": "...",
  "contextSummary": "...",
  "wasContextCompressed": true,
  "transfer": {
    "schemaVersion": "netmind.mindmap.v1",
    "title": "...",
    "nodes": [],
    "relations": []
  },
  "warnings": []
}
```

### 验证结果

- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
  - 当前环境未设置 `NETMIND_TEST_POSTGRES_CONNECTION`，数据库写入集成测试按脚本逻辑跳过。
- 已执行 `npm run build`，通过。
- 真实 AI 调用仍依赖 `DEEPSEEK_API_KEY` 或本机 Ollama 服务。

### 提交要求建议

- 建议分支名：`codex/p1-ai-cleaning`
- 建议 commit：`feat: add contextual AI requirement structuring`
- PR 说明：
  - 新增带上下文的 AI 需求结构化接口。
  - 长上下文按配置阈值先压缩再进入需求拆解。
  - 补强完整导图导入结构校验。
  - 前端 AI 请求等待时清空输出栏并显示状态提示。
  - 更新 AI Prompt 配置、Swagger 和 P1 开发日志。

## 2026-04-30 P1.4 中文本土化、日志系统与 AI 对话记录

### 本阶段目标

- 将前端显示文案改为中文，并尽量将后端返回错误翻译为中文。
- 新增日志系统和切面，记录接口调用、数据库非查询写操作和 AI API 调用。
- 新增 AI 对话记录表及增删改查接口。

### 完成内容

- 前端 `/src/NetMind.Frontend/src/App.vue`：
  - 阶段标识更新为 `P1.4`。
  - 顶部操作、导图、节点、关联、AI 清洗、导入导出、需求对话弹窗等界面文案改为中文。
  - API 错误兜底、表单校验、成功提示和等待状态改为中文。
  - 需求对话新增 `conversationId`，发送上下文对话时传给后端用于落库。
- 后端错误信息中文化：
  - Controller 常见 400/404 返回改为中文。
  - Service/Repository 主要业务校验和 AI 返回校验错误改为中文。
  - 全局数据库异常包装改为 `数据库请求失败：...`。
  - `ApiResult.Ok` 默认消息改为 `成功`。
- 日志系统：
  - 新增通用日志接口 `/src/NetMind.Common/Logging/IAppLogger.cs`。
  - 新增 WebAPI 日志实现 `/src/NetMind.WebApi/Infrastructure/AppLogger.cs`。
  - 新增接口调用切面 `/src/NetMind.WebApi/Middleware/ApiCallLoggingMiddleware.cs`：
    - 记录 HTTP 方法、路径、状态码、TraceId、耗时。
  - Repository 写操作日志：
    - 导图、节点、节点关联、AI 对话记录的新增、修改、删除均记录为 `存储层写操作`。
    - 查询类接口不记录为存储层写操作，避免噪声。
  - AI API 调用日志：
    - `AiCleanService` 调用 DeepSeek/Ollama 后记录模型、Provider、Endpoint、状态码和耗时。
  - 日志文件：
    - 默认写入 `/src/NetMind.WebApi/Logs/netmind-yyyyMMdd.log`。
    - 配置项：`AppLogging:FileEnabled`、`AppLogging:Directory`。
  - 配置文件新增 `Logging:LogLevel` 与 `AppLogging`。
- AI 对话记录表：
  - 新增实体 `/src/NetMind.Models/Entities/AiConversationRecordEntity.cs`。
  - 新增 DTO `/src/NetMind.Models/Dtos/AiConversationRecordDtos.cs`。
  - 新增 Repository：
    - `/src/NetMind.Repository/Interfaces/IAiConversationRecordRepository.cs`
    - `/src/NetMind.Repository/Implementations/AiConversationRecordRepository.cs`
  - 新增 Service：
    - `/src/NetMind.Services/Interfaces/IAiConversationRecordService.cs`
    - `/src/NetMind.Services/Implementations/AiConversationRecordService.cs`
  - 新增 Controller：
    - `/src/NetMind.WebApi/Controllers/AiConversationRecordsController.cs`
  - AI 上下文对话接口 `POST /api/ai/context-chat` 在传入 `conversationId` 时自动写入用户消息和 AI 回复两条记录。
  - Swagger 手写文档新增 AI 对话记录接口。
- 数据库脚本：
  - 新增 `/AI文档/SQL/P1.4_AddAiConversationRecord.sql`。
  - 表：`ai_conversation_record`。
  - 字段：`conversation_id`、`role`、`content`、`model_id`、`prompt`、`context_summary`、`was_context_compressed`、创建/更新时间、逻辑删除字段。
  - 索引：会话、角色、模型。

### 新增接口清单

- `GET /api/ai-conversation-records?conversationId=...`：查询 AI 对话记录列表，可按会话过滤。
- `GET /api/ai-conversation-records/{id}`：查询单条 AI 对话记录。
- `POST /api/ai-conversation-records`：创建 AI 对话记录。
- `PUT /api/ai-conversation-records/{id}`：更新 AI 对话记录。
- `DELETE /api/ai-conversation-records/{id}`：逻辑删除 AI 对话记录。

### 人工审核点

- AI 提示词：
  - 本阶段未调整 AI Prompt 内容，只新增对话记录落库能力和 AI API 调用日志。
  - 现有提示词仍位于 `AiClean:Prompt` 配置段。
- 上下文方法设计与实现：
  - 前端为每轮需求对话生成 `conversationId`。
  - `新对话` 会重置本地上下文和 `conversationId`。
  - 后端 `context-chat` 仍沿用 P1.3 的上下文压缩策略；当收到 `conversationId` 时，将用户消息和 AI 回复分别写入 `ai_conversation_record`。
  - 自动落库记录包含 AI 回复、模型 ID、最终 Prompt、上下文摘要和是否压缩上下文，便于后续追溯和审核。

### 验证结果

- 已执行 `dotnet build src\NetMind.sln -c Release --no-restore -v minimal`，通过，0 警告，0 错误。
- 已执行 `npm run build`，通过。
- 已执行 `dotnet run --project src\NetMind.IntegrationTests\NetMind.IntegrationTests.csproj -c Release --no-build`，通过。
  - 当前环境未设置 `NETMIND_TEST_POSTGRES_CONNECTION`，数据库写入集成测试按脚本逻辑跳过。

### 已知问题

- 当前环境未提供 PostgreSQL 测试连接，未实际执行 `P1.4_AddAiConversationRecord.sql` 后的数据库 CRUD 冒烟。
- 日志当前按日期写入本地文件和 ASP.NET Core 日志管道，尚未新增日志表或按大小切分策略。
- AI 对话记录自动落库只接入了 `POST /api/ai/context-chat`；独立 AI 清洗和需求结构化接口只记录 AI API 调用日志，未写入对话记录表。

### 提交要求建议

- 建议分支名：`codex/p1-localization-logging-ai-chat-records`
- 建议 commit：`feat: add localization logging and AI conversation records`
- PR 说明：
  - 前端主要显示文案和常见错误提示中文化。
  - 新增接口调用日志切面、存储层写操作日志和 AI API 调用日志。
  - 新增 `ai_conversation_record` 表、迁移脚本和 CRUD 接口。
  - AI 上下文对话接口支持按 `conversationId` 自动保存用户消息与 AI 回复。
  - 更新 Swagger、健康检查阶段和 P1 开发日志。
  - 验证：Release 后端构建、前端构建、集成测试通过；真实数据库 CRUD 冒烟需配置 PostgreSQL 后执行新增 SQL。
