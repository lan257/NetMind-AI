# NetMind P2 开发日志

更新时间：2026-04-30

## 2026-04-30 P2.0 前端重构

### 本阶段目标

- 引入统一前端组件库，固化 Vue3 前端风格。
- 拆分原单文件 App.vue，形成页面、组件、组合逻辑和接口服务边界。
- 重构主页面为“展示/工作台 × 图/列表”的 2×2 思维导图界面模型。
- 迁出新增导图页面，集中承载 AI 清洗、需求对话、导入导出和空白导图创建。

### 完成内容

- 前端依赖：
  - 新增 `element-plus`。
  - 新增 `@element-plus/icons-vue`。
  - `main.js` 全局引入 Element Plus 和样式。
- 前端结构：
  - `App.vue` 缩减为页面编排和全局状态接线。
  - 新增 `src/services/api.js` 统一封装 API 请求和下载跳转。
  - 新增 `src/composables/useMindMapWorkspace.js` 承载导图、节点、AI、导入导出和对话状态。
  - 新增多个组件：顶部栏、悬浮消息、视图切换、导图列表、图占位画布、层级节点列表、工作台编辑卡片、节点预览弹窗和新增导图页面。
- 主页面：
  - 始终保留“展示/工作台”和“图/列表”两个切换控件。
  - 始终展示思维导图列表。
  - 新增思维导图按钮迁移为进入新增导图页面的入口，并放在导图列表附近。
  - 移除主页面上的导图标题输入框和原地新增按钮。
  - 展示模式不显示数据库编辑按钮。
  - 工作台模式仅在列表视图显示右侧节点编辑卡片。
  - 图视图右侧为占位画布。
  - 列表视图右侧为层级节点列表，支持折叠/展开。
  - 点击节点弹出只读内容弹窗，不提供编辑操作。
- 新增导图页面：
  - 承载空白导图创建、AI 清洗、需求对话、结构导入导出和文件导入导出。
  - 提供图/列表节点展示切换，图视图暂为占位画布。
- 交互提示：
  - 原页面流内消息改为顶部悬浮提示，避免挤占列表、画布和表单布局。
- 文档：
  - 更新 `AI文档/开发规范.md`，写入 Element Plus 使用规范、App.vue 拆分规范和悬浮消息规范。

### 验证结果

- 已执行 `npm run build`，通过。
- 构建存在 Element Plus 引入后的 chunk 体积警告，不影响本次功能构建结果。
- 依赖安装过程中首次遇到 Windows `EPERM spawn`，已用 `--ignore-scripts` 重新安装成功；Element Plus 当前构建验证通过。
- `npm audit` 提示 2 个 moderate 级别依赖告警，本阶段未执行破坏性升级。

### 已知问题

- 图视图当前按 P2.0 要求为占位画布，尚未接入真实可视化导图库。
- 新增导图页面的图/列表切换复用当前选中导图数据，导入或创建成功后会刷新并选中新导图。
- 工作台保留节点新增、保存、删除和删除子树能力，但不再提供导图删除和关联编辑入口。

### 提交要求建议

- 建议分支名：`codex/p2-frontend-refactor`
- 建议 commit：`refactor: rebuild frontend workspace layout`
- PR 说明：
  - 引入 Element Plus 并写入前端开发规范。
  - 拆分 App.vue，新增组件、组合逻辑和 API 服务边界。
  - 重构主页面为展示/工作台与图/列表模型，列表支持层级折叠和只读节点弹窗。
  - 迁出新增导图页面，集中承载 AI 清洗、需求对话和导入导出。
  - 验证：`npm run build` 通过；存在依赖体积警告和 npm audit moderate 告警。

## 2026-04-30 P2.0 前端重构补充调整

### 调整内容

- 尊重人工调整结果，保留新增思维导图页面删除空白创建后的形态，并保留顶部删除刷新、接口文档按钮后的形态。
- 节点编辑卡片补回原有节点关联能力，包括目标节点、关系类型、权重、新增关联和删除关联。
- 节点编辑卡片补充更明确的中文提示和操作文案，减少“根节点/子节点/保存”等短按钮带来的歧义。
- 工作台列表下点击节点只选中并回填右侧编辑卡片，不再弹出只读内容弹窗。
- 展示列表下点击节点仍打开只读内容弹窗，弹窗调整为固定大尺寸。
- 新增思维导图页面移除“展示/工作台”切换，仅保留“图/列表”节点展示切换；AI 清洗和导入/导出两张卡片统一宽度、高度和文本区尺寸。

### 验证结果

- 已执行 `npm run build`，通过。
- 仍存在 Element Plus 引入后的 chunk 体积警告，不影响本次构建结果。

## 2026-04-30 P2.1 思维导图 Canvas 画布

### 本阶段目标

- 将图视图从占位画布升级为可绘制节点关系的 Canvas 思维导图。
- 复用现有导图和节点数据，不新增后端接口和第三方可视化依赖。

### 完成内容

- 新增 `src/components/MindMapCanvas.vue`：
  - 使用 HTML Canvas 绘制思维导图。
  - 以导图标题作为中央根节点。
  - 根据节点 `parentId`、`orderNo` 和 `id` 构建父子树。
  - 顶层节点左右分布，子节点按层级向外展开。
  - 绘制高清网格背景、圆角节点、中心根节点、父子曲线连线和选中态。
  - 支持窗口尺寸变化后自动重绘，兼容高 DPI 屏幕。
  - 支持点击节点，展示模式打开只读预览，工作台模式仅选中节点。
- 主页面图视图接入 `MindMapCanvas`，替换原 `MapCanvasPlaceholder`。
- 新增导图页面的节点展示图视图同步接入 `MindMapCanvas`。
- 删除旧的占位画布组件和对应占位样式。

### 验证结果

- 首次执行 `npm run build` 被 PowerShell 执行策略拦截 `npm.ps1`。
- 改用 `npm.cmd run build` 后，沙箱内遇到 esbuild `spawn EPERM`。
- 经用户授权在沙箱外执行 `npm.cmd run build`，构建通过。
- 仍存在 Element Plus 引入后的 chunk 体积警告，不影响本次构建结果。

### 补充修复

- 修复 Canvas 画布持续向下延展问题：
  - 原因是绘制时读取带边框的 `getBoundingClientRect()` 高度后再写回 Canvas 样式高度，子元素和父容器之间形成 ResizeObserver 重绘反馈。
  - 改为使用画布容器的 `clientWidth/clientHeight` 设置绘制缓冲区，并由 CSS 固定容器高度。
- 已重新执行 `npm.cmd run build`，通过；仍存在既有 chunk 体积警告。

### 提交要求建议

- 建议分支名：`codex/p2.1-mind-map-canvas`
- 建议 commit：`feat: draw mind map nodes on canvas`
- PR 说明：
  - 新增 Canvas 思维导图组件，绘制根节点、层级节点和曲线连线。
  - 主页面和新增导图页面图视图替换占位画布。
  - 保留节点点击选中和展示预览交互。
  - 验证：`npm.cmd run build` 通过；存在既有 chunk 体积警告。

## 2026-05-05 P2.2 前端响应式尺寸优化

### 本阶段目标

- 优化前端在不同屏幕宽度下的知识卡片弹窗、需求对话弹窗、按钮和数字输入框尺寸表现。

### 完成内容

- 展示-列表：
  - 点击节点打开的知识卡片弹窗宽度改为基于视口的动态宽度。
  - 知识卡片弹窗高度使用视口约束动态调整，内容过多时在弹窗内部滚动。
- 新增思维导图：
  - 需求对话弹窗宽度改为基于视口的动态宽度，并限制最大高度，避免小屏溢出。
  - 对话消息区域改为动态最大高度，内容过多时在弹窗内部滚动。
  - AI 清洗、导入导出和需求对话底部按钮统一为相邻按钮等宽，小屏下自动单列排列。
- 工作台-列表：
  - 节点编辑按钮统一填满所在网格列，修正 Element Plus 默认按钮间距导致的宽度不一致。
  - 数字输入框宽度改为填满容器，与相邻表单控件和下方操作按钮保持一致。

### 验证结果

- 已执行 `npm.cmd run build`，通过。
- 仍存在 Element Plus 引入后的 chunk 体积警告，不影响本次构建结果。

### 提交要求建议

- 建议分支名：`codex/p2.2-responsive-frontend`
- 建议 commit：`fix: optimize responsive frontend sizing`
- PR 说明：
  - 优化展示列表节点知识卡片弹窗响应式尺寸。
  - 优化新增思维导图需求对话弹窗和按钮等宽布局。
  - 统一工作台列表按钮和数字输入框宽度。
  - 验证：`npm.cmd run build` 通过；存在既有 chunk 体积警告。
